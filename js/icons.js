import { createIcons, icons } from 'lucide';
import {Menu, ArrowRight, Globe, Users, User, Delete, Files, Settings, Mails, DoorClosed, X} from "lucide";

createIcons({ icons, nameAttr: 'data-icon', attrs: { class: ['iconset'] } });

createIcons({
  icons: {
    Menu,
    ArrowRight,
    Globe,
    Users,
    User,
    Delete,
    Files,
    Settings,
    Mails,
    DoorClosed,
    X
  },
});